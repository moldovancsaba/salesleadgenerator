# Roadmap — Sales Lead Generator

**Why this file exists:** GitHub's Projects (v2) board for this repo cannot be read or written from this session — its only API is GraphQL, and this session's GitHub credentials are restricted to a pinned set of PR-review operations (verified directly: a live authenticated GraphQL call was rejected by the session's own proxy, not by GitHub). Classic Projects (the old REST-based board) is gone from GitHub entirely — sunset 2024, fully removed mid-2025 (confirmed live: `GET /repos/.../projects` returns a real `404` from GitHub itself). This file is the substitute: every real open issue, grouped by status, kept in sync by hand whenever an issue's state changes. It is not a replacement for the issues themselves — every row here is a real GitHub issue, this is just a single-page view of all of them.

**Keeping this in sync:** whenever an issue opens, closes, or changes status/priority, update its row here in the same change. If this file and GitHub ever disagree, GitHub is the source of truth — fix this file, not the other way around.

**Full methodology** — the exact tools used to manage issues, the mandatory issue-body structure, the real label taxonomy, how dependencies between issues are recorded, and the complete verified investigation behind why no board is reachable — lives in `docs/ISSUE_MANAGEMENT.md`. This file is just the live status view; that one is the reference.

**Not the same file as `_archived/roadmap.md`** — that's a frozen, historical feature-status log (v2.4.61), superseded by `CHANGELOG.md`, sharing this file's basename by coincidence rather than by relation. See `README.md`'s "Archived Documentation" table.

Last synced: 2026-09-25, re-verified directly against `moldovancsaba/salesleadgenerator`'s real GitHub issue state (`list_issues`/`issue_read`, not assumed from a prior sync or from commit messages alone). This sync corrects a real staleness this file had accumulated: PR #218 — which every row below previously described as "open/draft, not yet merged" — had in fact already merged to `main`, and GitHub had already auto-closed #125/#198–#201/#203–#206/#209/#212/#213/#215–#217 as a result (each landed via a commit whose message referenced its own issue number). None of that had been reflected here. #208 (kanban real drag-and-drop) shipped and closed in this same sync, pushed directly to `main` per standing owner instruction. #210 remains open, genuinely partial (Phase 1 of 6 only).

---

## In Progress

| # | Title | Priority | Notes |
|---|---|---|---|
| [#132](https://github.com/moldovancsaba/salesleadgenerator/issues/132) | Backfill existing leads into the controlled taxonomy schema (rulebook v1.0, Phase 2) | P1 | 318 of 2,874 leads classified as of the last batch (2026-08-08), re-derived live from `GET /api/leads` rather than trusted from any prior branch's own claim (see `docs/LESSONS_LEARNED.md` §3 — an orphaned branch's claimed progress was found not to match production). Picking now weighted toward QUALIFIED/ENGAGED/PROPOSAL-stage leads (sales-team-active), not just backlog — see `docs/LEAD_TAXONOMY_MIGRATION_PLAN.md` §9. Real, ongoing, multi-session effort — not completable in one sitting. |

## Ready (unblocked, unstarted)

| # | Title | Priority | Notes |
|---|---|---|---|
| [#202](https://github.com/moldovancsaba/salesleadgenerator/issues/202) | Ops: Inbound email activation - DNS cutover and live verification of the built webhook | P1 | Follows closed #141/#142 (webhook + reply matching, fully built and tested, never live). Needs owner-only steps: `MX leads.haho.ai` DNS record, `RESEND_API_KEY`/`RESEND_WEBHOOK_SECRET` in Vercel production, re-verification of the existing Resend `email.received` webhook subscription, and (for the intended automatic flow) a Google Workspace routing rule. **Needs the owner**, not another agent turn — this session has no access to any of the four systems involved. |
| [#228](https://github.com/moldovancsaba/salesleadgenerator/issues/228) | Security: integrations OAuth callback trusts an unsigned state cookie for brand, tenant and user | P2 | Found by the 2026-09-26 auth audit. Fix: session check at the callback plus a server-side state record. |
| [#229](https://github.com/moldovancsaba/salesleadgenerator/issues/229) | Security hardening: global settings writable by any SSO login, public-endpoint list incomplete, booking and quote-view hardening | P3 | Five independent low/medium items from the 2026-09-26 auth audit; each can ship alone. |
| [#165](https://github.com/moldovancsaba/salesleadgenerator/issues/165) | New-user onboarding tour: step-by-step spotlight walkthrough (design record) | P3 | Design plan, decision made 2026-08-08 (`driver.js`). Implementation (#185) shipped 2.4.187 — this issue stays open by explicit owner design as the permanent design record, not because anything remains to be done. |
| [#210](https://github.com/moldovancsaba/salesleadgenerator/issues/210) | API: Public integration surface - OAuth-scoped API access and outbound webhooks (parent tracking issue) | P3 | Split into two real sub-issues, per its own §24 sequencing: [#219](https://github.com/moldovancsaba/salesleadgenerator/issues/219) (outbound webhook system) shipped 2.4.210, see Recently closed below; [#220](https://github.com/moldovancsaba/salesleadgenerator/issues/220) (migrate cron jobs + research agent to scoped keys, retire `SLG_API_KEY`) remains genuinely owner-blocked — see Blocked below. This parent issue itself stays open until #220 also closes. |

## Blocked

| # | Title | Priority | Notes |
|---|---|---|---|
| [#137](https://github.com/moldovancsaba/salesleadgenerator/issues/137) | Duplicate lead records at scale: 43.8% of Seyu, 10.7% of CogMap | P1 | All code gaps shipped: matching fix (diacritics/tight key) earlier; 2.4.225 raised the scan cap to 5000 (CogMap is 2,308 leads, its oldest ~300 were never scanned), stopped re-scans resurfacing decided pairs after a merge, and added `unresolvedPairs` so a re-scan shows the backlog shrinking. **Needs the owner** (super-admin SSO browser session at `/admin/duplicates`, no API-key path): run the scan for Seyu and CogMap, review and merge pairs, and decide whether the sport gate should treat a missing sport as a wildcard for identical names (DAZN/Sphere cases on the issue). |
| [#221](https://github.com/moldovancsaba/salesleadgenerator/issues/221) | `gds-compliance` `forbidden-color` rule false-positives on GitHub issue-number references (`#NNN`) | P3 | In-repo part shipped 2.4.222: the one real finding (`app/layout.tsx` theme-color) moved to `lib/theme/app-chrome-colors.ts`; root cause corrected (issue references inside test-title *strings*, not comments — the tool strips comments). 27 known findings remain, all test titles, listed in `docs/STACK_AND_DEPENDENCIES.md`. **Needs the owner**: file the drafted upstream report at `sovereignsquad/general-design-system` (draft posted on #221), and optionally say whether to drop `#` from the 27 test titles instead. |
| [#224](https://github.com/moldovancsaba/salesleadgenerator/issues/224) | Outage: every Vercel Cron job returns 401 in production because CRON_SECRET is not set | P1 | Found 2026-09-26, verified against the live Vercel env-var list (no `CRON_SECRET`) and runtime logs (6 of 6 hourly runs → 401 across three deployments). Nothing scheduled has been running: forecast snapshots, ticket-size recalc, cadence/automation ticks, report delivery, Gmail sync, webhook delivery. The code side shipped in 2.4.219 (a clear log line on rejected cron calls, docs corrected). **Needs the owner**: add `CRON_SECRET` (Production, Sensitive) in the Vercel dashboard and redeploy — owner confirmed they will do this themselves; verify via runtime logs afterwards. Since 2.4.221 the cadence and report crons hold due email work while `RESEND_API_KEY` is unset (it is also missing in production), so enabling the crons first no longer loses data; email still only goes out once `RESEND_API_KEY` is set (see #202). |
| [#220](https://github.com/moldovancsaba/salesleadgenerator/issues/220) | API: Migrate cron jobs and the research agent to scoped keys; retire SLG_API_KEY | P3 | Sub-issue of #210. Generating the actual scoped keys is code-reachable (the admin API already shipped in #210 Phase 1), but using them for real requires setting new environment variables in the actual deployed Vercel project and updating the external research agent's own separate deployed config — neither reachable from this sandbox. **Needs the owner**, not another agent turn — same class of block as #202/#137. |

## Backlog

| # | Title | Priority | Notes |
|---|---|---|---|
| [#193](https://github.com/moldovancsaba/salesleadgenerator/issues/193) | Non-conforming `lead.source` values: 2,739 of 3,051 leads don't match the documented channel vocabulary | P3 | Not a validation gap — `source` is deliberately freeform (issue #123). Data-quality backlog: `csv_import` (1730), missing/`unknown` (977), and a handful of dvsc process-artifact strings need an owner decision on reclassification before any bulk write. Numbers re-derived live against production 2026-08-20, reconciled exactly against `GET /api/health`'s lead counts. |
| [#222](https://github.com/moldovancsaba/salesleadgenerator/issues/222) | Data quality: batch of Seyu leads created 2026-07-18 have country/region hardcoded to "DE" regardless of actual location | P2 | Found 2026-09-25 while picking issue #132's next batch: 7 of a 20-lead sample of Seyu's QUALIFIED column had `country: "DE"`, only 1 (FC Bayern Munich) correct. 6 confirmed-wrong ones fixed same day (2.4.215) — MotorLand Aragón, Leicester City FC, Nottingham Forest FC, Sporting Clube de Portugal, Olympique de Marseille, Feyenoord Rotterdam — plus Celtic FC (2.4.213). **Full scope not yet quantified** — a full paginated scan is blocked by this session's own production-reads safeguard; needs an owner-run script or a session that can do a bulk scan. |
| [#223](https://github.com/moldovancsaba/salesleadgenerator/issues/223) | Data quality: invalid/non-ISO country codes on multiple Seyu leads, distinct from issue #222's DE-cluster | P2 | Found 2026-09-26 while quantifying #222: a second, distinct bad-`country` signature — non-ISO codes (`EA`/`LO`/`SO`/`SP`) or a real-but-wrong country's code (`NE`/Niger, `NO`/Norway), consistent with a "first 2 letters of a free-text `region` string" derivation bug. 8 confirmed-wrong leads fixed same day (2.4.216) — Northampton Saints, Saracens RFC, Gloucester Rugby (`GB`), Valencia Basket Club, LALIGA (`ES`), PSV Eindhoven (`NL`), AEG Presents, Live Nation Entertainment (`US`). **Full scope not yet quantified** — same bulk-read constraint as #222. |

## Recently closed (context)

| # | Title | Resolution |
|---|---|---|
| [#227](https://github.com/moldovancsaba/salesleadgenerator/issues/227) | Bug: outreach send/log, templates, automation rules, cadences and battlecards cannot be saved from the browser (401), and their list reads are unauthenticated | Fixed 2.4.227, pushed to `main`. `requireBrandAccessApi` on every handler, brand-filtered by-id lookups, recipients of a one-off send taken from the stored lead. Open owner decision (admin role for cadence/rule management) noted on the issue. |
| [#226](https://github.com/moldovancsaba/salesleadgenerator/issues/226) | Security: sales settings, product catalog, outcome logs and outreach logs are readable/writable without any credential | Fixed 2.4.226, pushed to `main`. Found by the 2026-09-26 route-by-route auth audit (each finding independently verified). Sales settings and products now use `requireBrandAccessApi` (browser session with brand access, scoped key, or legacy key); the two log reads use `requireApiKey` (no in-app caller). Tests fail against the previous code. |
| [#225](https://github.com/moldovancsaba/salesleadgenerator/issues/225) | Bug: every Lead Detail save (MODIFY) silently demoted CHECKED/VERIFIED qualityStatus to DRAFT | Fixed 2.4.224, pushed to `main`. Found by independent review during the #193 triage, reproduced with failing integration tests, fixed by applying the quality ceiling only when the request itself sets `qualityStatus`. Already-demoted leads cannot be identified retroactively. |
| [#219](https://github.com/moldovancsaba/salesleadgenerator/issues/219) | API: Outbound webhook system - signing, delivery worker, retry/dead-letter, admin UI | Shipped (2.4.210), pushed directly to `main`. Sub-issue of #210. New `webhooks`/`webhook_deliveries` Mongo collections; SSRF defenses reuse issue #69's already-audited `isPrivateOrReservedIp()` (`lib/tech-stack-scan.ts`) rather than reimplementing them, applied at registration and again defensively before every delivery (DNS-rebinding/TOCTOU defense); HMAC-SHA256 signing mirrors `lib/resend-webhook.ts`'s inbound verification shape in the outbound direction — full integrator recipe in new `docs/WEBHOOKS.md`. Webhook secret encrypted at rest via `lib/integration-crypto.ts`, reusing issue #217's existing `INTEGRATION_CREDENTIALS_ENCRYPTION_KEY` — no new env var. Retry schedule (1m/5m/30m/2h/12h, auto-disable after 5 consecutive exhausted deliveries) matches #210 §11 exactly; delivery-worker cron cadence resolved by real verification against this project's live Vercel config (two already-working hourly crons proving at least hourly-cron eligibility), not assumed — ships hourly, disclosed as a departure from the issue's own every-minute suggestion. Admin UI extends the existing `/admin/api-keys` page rather than a new route. **Disclosed gap**: no real, publicly reachable receiver endpoint exists in this sandbox — every delivery test runs against an injected network stub (real DNS/HTTP boundary replaced deterministically), not a real TLS handshake against a real third party; an operator with a real receiver should confirm one real signed delivery before production reliance. See `docs/ARCHITECTURE.md`'s "Outbound Webhooks" section for full detail. |
| [#211](https://github.com/moldovancsaba/salesleadgenerator/issues/211) | Deals: Quote generation - line-item documents with e-signature status tracking | Shipped (2.4.209), pushed directly to `main`. New `quotes` Mongo collection + a server-enforced `draft → sent → viewed → signed` state machine (`sent → signed` also directly allowed; `expired` reachable from any pre-`signed` state) off `lib/deals.ts`'s `Deal`, PDF via `@react-pdf/renderer` (real, extractable, never-rasterized text — verified by unit test), stored via `@vercel/blob` under `access: 'private'` — a deliberate improvement over the issue's own `access: 'public'` pseudocode, verified against the real installed SDK's type definitions; the PDF is reachable only through this app's own `shareToken`-gated, rate-limited view route, never a bare Blob URL. Sent via the existing `lib/outreach-send.ts` Resend path (new `'quote'` source kind, no parallel email code). No native e-signature capture — a token-gated shareable link the recipient can "sign" (public route) or the rep can mark signed manually; a third-party e-signature integration remains an explicit fast-follow, not built here. **Disclosed gap**: this issue's own §19/§20 explicitly require generating a real quote against a real Vercel Blob store and opening the resulting PDF — no `BLOB_READ_WRITE_TOKEN`/provisioned Blob store exists in this sandbox, so that real end-to-end round-trip could not be exercised; covered instead by unit tests against `renderQuotePdf()`'s real output bytes and integration tests with `@vercel/blob` mocked at the SDK-export boundary. See `docs/ARCHITECTURE.md`'s "Quotes" section for full detail. |
| — | Security: two critical Next.js RCE CVEs (GHSA-p293-qw3h-jr36, GHSA-2xp9-vwfh-vxw4) | Fixed (2.4.208) — found via `npm audit` while installing unrelated new dependencies for issue #211, confirmed unrelated to them, fixed same-day as its own separate change: `next`/`eslint-config-next` 16.3.0 → 16.3.6 (patch-level), `vitest`/`@vitest/coverage-v8` 4.1.10 → 4.1.11 (a co-discovered moderate CVE, also patch-level). No GitHub issue filed — already fully resolved, full detail in `docs/STACK_AND_DEPENDENCIES.md`'s Dependency Audit section and `CHANGELOG.md` 2.4.208. |
| [#214](https://github.com/moldovancsaba/salesleadgenerator/issues/214) | Filters: Shareable saved views - synced, team-visible filter presets | Shipped (2.4.207), pushed directly to `main`. Moved saved filter presets (`lib/saved-filters.ts`'s pure logic reused unchanged, plus `MAX_SAVED_FILTERS` now exported) from per-browser `localStorage` to a server-persisted `saved_filters` Mongo collection (new `lib/saved-filters-store.ts`, new `app/api/saved-filters*` routes) keyed `{brand, ssoUserId, name}`, plus an optional `sharedWithBrand` flag gated on the real brand-`admin` role via `getRoleForBrand` (already folds in the super-admin bypass). Ownership enforced strictly server-side — a non-owner, even a brand/super admin, gets 403 on `PATCH`/`DELETE`. A one-time, explicit "Import your local saved filters" migration banner never silently discards existing `localStorage` data. **Disclosed gap**: a full authenticated click-through of the Filters drawer (real SSO login required) could not be performed in this sandbox — `next build` and the full automated test suite (1080 unit/442 integration, including 22 new saved-filters-specific integration tests) are the verification this session could actually run. |
| [#208](https://github.com/moldovancsaba/salesleadgenerator/issues/208) | Kanban: Real drag-and-drop - GDS `enableDrag` activation wired to persisted stage and order changes | Shipped (2.4.206), pushed directly to `main` per standing owner instruction. `enableDrag` re-enabled behind a new Mongo-backed kill switch (`settings.{key:'kanban_drag_enabled'}`, defaults `false`/fail-closed, no UI — toggled via `PUT /api/settings`, same precedent as `wipLimits`). Cross-column drag reuses the existing `COLUMN_MOVE` action unchanged. New `COLUMN_REORDER` action persists a real same-column reorder for the 5 manually-controlled columns via fractional-indexed `sortOrder` (`lib/kanban-reorder.ts`), with a bounded whole-column resequencing fallback once float64 precision is exhausted; a same-column drag on DISCOVERED/QUALIFIED is rejected with a visible notification instead of the previous silent no-op. The board mount is now wrapped in `ErrorBoundary`. No new dependency (`@dnd-kit/*` stays exclusively inside `gds-core`'s own tree; the pre-existing direct `package.json` entries for it predate this issue). **Disclosed gap**: real-device pointer/touch/keyboard drag verification — the issue's own §19 calls this "the load-bearing verification step for this entire issue, not a formality" — could not be performed in this sandbox and remains required on a staging deployment before the kill switch is turned on in production. |
| [#217](https://github.com/moldovancsaba/salesleadgenerator/issues/217) | Integrations: Third-party connection hub - admin-managed credentials for Google Workspace and Calendly | Shipped, merged to `main` via PR #218. New `integration_connections` collection, AES-256-GCM encryption at rest (fails closed in every environment), generalized OAuth2 connect/callback for Google Calendar/Gmail/Contacts, verify-before-store API-key flow for Calendly. Admin UI at `/salessettings/[client]/integrations`. A real RFC 6265 cookie-encoding bug (unescaped JSON in a cookie value) was found and fixed before shipping. **Disclosed gap**: three manual verification steps (a real Google account, a real external revoke, a real Calendly token) could not be performed in this sandbox — the feature fails closed until an operator provisions the required env vars. |
| [#216](https://github.com/moldovancsaba/salesleadgenerator/issues/216) | Integrations: Gmail and Google Contacts sync - two-way email and contact data flow | Shipped, merged to `main` via PR #218, built on #217's hub. Gmail messages to/from a known lead contact ingested into `activityLog` (`source: 'gmail-sync'`) via an hourly cron, headers-only-then-body-on-match; a rep-initiated "Import from Google Contacts" action on lead detail. Zero new npm dependencies. **Real, disclosed architectural mismatch**: shipped against the hub's real one-connection-per-brand schema rather than the issue's assumed per-rep model. **Disclosed gap**: a real end-to-end run against a real Gmail/Google Contacts account could not be performed in this sandbox. |
| [#207](https://github.com/moldovancsaba/salesleadgenerator/issues/207) | Calendar: Meeting scheduler - booking links and two-way calendar sync | Shipped, merged to `main` via PR #218, built directly on #217's hub — zero new env vars. Public `/schedule/[brand]` booking page (DST-correct slot math), race-safe booking via a short-lived unique-indexed slot-claim document, a new DB-backed rate limiter (this repo's first public unauthenticated routes). **Real, disclosed architectural mismatch, recurring from #216**: shipped as one shared booking calendar per brand rather than the issue's assumed per-rep model. **Disclosed gap**: a real end-to-end run against a real Google account/booked event could not be performed in this sandbox. |
| [#213](https://github.com/moldovancsaba/salesleadgenerator/issues/213) | UX: Kanban card density - information hierarchy, WIP signaling, and command palette | Shipped, merged to `main` via PR #218. Card density collapses to a fixed Tier 0 plus an in-card "Show more" expand; non-blocking WIP-limit header badge (`/api/settings` `wipLimits`); GDS's real `CommandRegistryProvider`/`useCommandLauncher`/`CommandPalette` adopted as-is. Disclosed deviation: the provider mounts unconditionally rather than desktop-gated at the provider level, per the issue's own sanctioned alternative. |
| [#212](https://github.com/moldovancsaba/salesleadgenerator/issues/212) | Reporting: Ad-hoc report builder - user-defined reports with scheduled delivery | Shipped, merged to `main` via PR #218. Closed-allowlist {metric, group-by, filters, date range} form driving a pure Mongo aggregation builder (zero raw-query-injection surface), `report_definitions` + optional recurring delivery via a new hourly cron. GDS's governed `GdsBarChart`/`GdsLineChart` adopted. Closed a real, verified pre-existing gap: added the missing `{tenantId, createdAt}`/`{tenantId, kanbanColumn}` indexes. |
| [#209](https://github.com/moldovancsaba/salesleadgenerator/issues/209) | Accounts: Company object - navigable parent-organization record with rollup | Shipped (Phase 1, as specced), merged to `main` via PR #218. Rep-navigable `/accounts/[brand]` view with a computed rollup (lead count, stage breakdown, pipeline/won value, contact count, recent activity) over the existing `Lead.parentOrgId` grouping — no new collection, no migration; a real `accounts` collection/merge-queue UI deferred to a follow-on issue. A real spec contradiction (currency handling, §9 vs §15) resolved by following the issue's own more specific rule, disclosed in `docs/ARCHITECTURE.md`. |
| [#206](https://github.com/moldovancsaba/salesleadgenerator/issues/206) | Contacts: Buying-committee roles - replacing isDecisionMaker with a role enum | Shipped, merged to `main` via PR #218. Additive `buyingRole` enum per contact resolved inside `lib/contacts.ts`'s shared `normalizeContact()`; `isDecisionMaker` stays accepted on input, now permanently derived on output. Idempotent backfill shipped — correctness doesn't depend on running it. |
| [#205](https://github.com/moldovancsaba/salesleadgenerator/issues/205) | Outreach: One-off tracked email send - decoupled from cadence automation | Shipped, merged to `main` via PR #218. `dispatchOutreachEmail()` generalized core; new `sendManualEmail()` + `POST /api/outreach-send`. A real send-timeline duplication bug (the Activity tab would have double-rendered a send) found and fixed before shipping. **Disclosed gap**: the real-send manual-verification step is owner-blocked (no `RESEND_API_KEY` in this sandbox). |
| [#204](https://github.com/moldovancsaba/salesleadgenerator/issues/204) | Forecast: Rep-editable probability - forecast categories and quota attainment tracking | Shipped, merged to `main` via PR #218. Rep-editable, sticky `Lead.forecastCategory` override feeding an additive `categoryWeightedRevenue` alongside the unchanged stage-weighted total. Quota-tracking half, unblocked by #198, implemented for real: `quota_targets` collection, `GET/PUT /api/quota/[brand]` (super-admin), `GET /api/quota/[brand]/attainment`. |
| [#203](https://github.com/moldovancsaba/salesleadgenerator/issues/203) | Kanban: Bulk actions v2 - contextual toolbar, undo, and bulk reassignment | Shipped, merged to `main` via PR #218. Bulk `FIELD_EDIT` (tags/qualityStatus) and `ASSIGN` (unblocked by #198); Mongo-backed, TTL-indexed, CAS-checked `bulkActionUndoTokens` + `POST /api/leads/bulk/undo`; "Select all in column", "Edit field…", "Reassign…", and a real Undo control in `app/kanban.tsx`. |
| [#201](https://github.com/moldovancsaba/salesleadgenerator/issues/201) | Automation: Workflow engine - trigger-action rules generalizing cadence-tick and stage-gate | Shipped, merged to `main` via PR #218. New `automation_rules` trigger→action engine generalizing `lib/cadences.ts`'s tick model. `lead_created`/`lead_moved_to_column` fire synchronously (the latter only after the stage gate passes); `stale_no_activity` runs on a new daily cron, idempotent per UTC day. `lead_assigned` kept schema-defined-but-unreachable, per the issue's own explicit scope. |
| [#200](https://github.com/moldovancsaba/salesleadgenerator/issues/200) | Activity: Manual call logging - structured Call activity type with outcome disposition | Shipped, merged to `main` via PR #218. `activityLog` gains a `type: 'call'` variant (closed `CallDisposition` enum, `loggedBy` from the verified session); new `POST /api/leads/[id]/activity`; logging a call touches `updatedAt` so it counts toward staleness. |
| [#199](https://github.com/moldovancsaba/salesleadgenerator/issues/199) | CRM: Team visibility - manager role and team-scoped pipeline access | Shipped, merged to `main` via PR #218, built directly on #198. New `teams` collection + `lib/teams.ts`; `assignedTo=team` filter on `GET /api/leads`/`GET /api/leads/columns`; new `GET/POST /api/admin/teams` + `PATCH/DELETE /api/admin/teams/[teamId]`; `/admin/teams` UI; "My Team" toggle in `FilterBar`. |
| [#198](https://github.com/moldovancsaba/salesleadgenerator/issues/198) | CRM: Lead ownership - assignedTo field, My Leads view, and reassignment workflow | Shipped, merged to `main` via PR #218. **Foundational for #199/#203/#204.** `Lead.assignedTo`/`assignedToEmail`/`assignedAt`/`assignedBy`; session-gated `ASSIGN` action in `executeLeadAction()` (self-assign always allowed, cross-user reassignment gated to brand admins, 403 on block); `assignedTo=me\|unassigned\|<ssoUserId>` filter; new `GET /api/leads/assignable-users`; "My Leads" toggle. |
| [#215](https://github.com/moldovancsaba/salesleadgenerator/issues/215) | Catalog: Product and price book - reusable line items feeding Deals and quotes | Shipped, merged to `main` via PR #218. New per-brand/tenant `products` collection promoting Sales Settings' free-text `ProductLine[]` into a priced, selectable catalog; `Deal.lineItems` with server-derived `value`/`source: 'catalog_line_items'`. One-time idempotent backfill, never clobbers a manually-edited row. New `/admin/products/[brand]` CRUD screen and a "Build from catalog" mode in the deal editor. Prerequisite for #211's deferred multi-line-item quote support. |
| [#125](https://github.com/moldovancsaba/salesleadgenerator/issues/125) | Adopt GDS zone-based kanban scroll routing (`columnPanZone`); retire local wheel-passthrough workaround | Shipped, merged to `main` via PR #218. `app/kanban.tsx`'s `<GdsKanbanBoard>` sets `columnPanZone="header"`; `lib/desktop-scroll-passthrough.ts` and its test deleted (grep-confirmed no other importer). Real-device trackpad confirmation still needs the owner — disclosed in `docs/OPERATOR_GUIDE.md`'s Known Issues. |
| [#197](https://github.com/moldovancsaba/salesleadgenerator/issues/197) | Near-zero lead review rate: add bulk Accept + inline card Accept/Decline to the kanban | Shipped (2.4.188). `PATCH /api/leads/bulk`'s `ALLOWED_BULK_ACTIONS` widened to include `ACCEPT`; kanban toolbar gained a bulk "Accept selected" button; `app/card.tsx` gained inline per-card Accept/Decline, wired through a new `onAction` prop to the existing `handleAction`/`PATCH /api/leads` path. Purely additive UI wiring onto already-audited backend paths (`leads-patch-actions.integration.test.ts`) — no data model or stage-gate change. Full gate green: 743/743 unit, 228/228 integration, 0 `tsc`/lint errors, 0 new `audit:gds-style` violations (stash A/B against the 26 pre-existing). |
| [#185](https://github.com/moldovancsaba/salesleadgenerator/issues/185) | Implement new-user onboarding tour using driver.js | Shipped (2.4.187, `4ba527a`, merged to `main`). All 7 steps from #165's design, two real UI-driving transitions (click-simulated, not just spotlight-and-describe on closed elements). Two real `driver.js` gotchas found by reading its compiled source and designed around: the auto-trigger is route-gated (a fresh login lands on `/forecast/[brand]`, not `/sales/[brand]` where the tour lives) and `onDestroyStarted` must call `driver.destroy()` itself or Escape/close silently no-ops. Storage is server-persisted (`SsoUserAccessRecord.tourSeenAt`), not localStorage. New `tests/lib/tour-logic.test.ts` (8 tests) for the pure logic. Authenticated auto-trigger path needs a real SSO login to fully exercise, disclosed — same limitation as #195/#196. Full gate green including a clean `next build`. |
| [#196](https://github.com/moldovancsaba/salesleadgenerator/issues/196) | Site-admin UI to create a new client/brand (no-deploy onboarding) | Shipped (2.4.186, `118be98`, merged to `main`). `/admin/clients` create form + `GET`/`POST /api/admin/clients`, uniqueness via `brands`'s own unique index. Along the way, found and fixed a real bug already live on `main` since #195: `app/lib/brand.ts` importing `lib/mongodb.ts` broke the client bundle for any Client Component pulling a real value (not just a type) from it — confirmed via `next build` that `/salessettings/[client]` was already broken by the #195 merge itself. Fixed by splitting pure constants into `app/lib/brand-constants.ts` + adding `server-only` to `brand.ts`. Migration script (`scripts/migrate-brands-to-mongo.ts`, from #195) still not run against production — no live Mongo access from this session; disclosed in both issues' resolution comments. Full gate green including a clean `next build`. |
| [#195](https://github.com/moldovancsaba/salesleadgenerator/issues/195) | Brand config becomes Mongo-backed (site-admin onboarding, backend plumbing) | Shipped (2.4.185, `fff6644`, merged to `main`). New `brands` Mongo collection + async `getBrandConfig`/`getAllBrandConfigs`/`resolveBrand`/`getForbiddenTermsFor`; grep-verified 51-file call-site sweep; forecast-model routing and forbidden-vocabulary derivation moved off hardcoded brand-name checks. Migration script not yet run against production (no live Mongo access from this session) — app runs correctly off the fallback config until it is. Full gate green. Prerequisite for #196, shipped in the same session. |
| [#194](https://github.com/moldovancsaba/salesleadgenerator/issues/194) | `POST /api/search-learning` 500s on first write for a new `companyId` (Mongo upsert path conflict) | Fixed (2.4.184) — `$setOnInsert` seeded `searchRuns`/`lastQueries` while `$inc`/`$push` targeted the same paths; MongoDB rejects that (code 40) so the insert branch never ran. Both seeds removed; the operators initialize their own fields. Found live while verifying #192, predates it. New `tests/integration/search-learning.integration.test.ts` verified to fail against the pre-fix code (3 of 4 cases 500), not merely pass after. |
| [#192](https://github.com/moldovancsaba/salesleadgenerator/issues/192) | Unauthenticated read/write exposure across search, boards, forecast, metrics, win-rates, ticket-size-calibration, and settings | Fixed (2.4.183, `ad53eaa`) — same gap class as #104/#178, found not to have generalized (`docs/LESSONS_LEARNED.md` §3). `requireBrandAccessApi` on 7 brand-scoped GET routes + `GET /api/search`'s brand mode; `requireSuperAdminSession` on `/api/search`'s no-brand cross-all-brands mode; new `requireApiKeyOrSession` guard on `PUT /api/settings`/`POST /api/search-learning`. Full gate passing and live-verified against a local dev server with real credentials before push. Production deploy status unconfirmed (no reachable Vercel account from this session). |
| [#188](https://github.com/moldovancsaba/salesleadgenerator/issues/188) | Per-field provenance on leads: `fieldVerifications` with a closed method enum | Merged to `main` (`03446dc`) and closed on GitHub. Additive only — new optional field at two scopes (lead-level for scalar fields, per-contact for contact fields), closed nine-value method enum, bounded by last-write-wins per `(field, method)` plus a 60-entry cap. |
| [#189](https://github.com/moldovancsaba/salesleadgenerator/issues/189) | `main` is red: credential scrub left webhook test fixtures undecodable | Fixed (PR #190) — the 2026-08-14 scrub replaced webhook test fixtures with a placeholder that isn't valid base64, so `standardwebhooks`' eager decode threw before any assertion ran (14 tests failing). Fixture is now derived in-file from a fixed byte pattern: decodable, deterministic, obviously synthetic, no literal for a scanner to match. Also restored two negative tests that had been given the same placeholder as the valid secret and would have passed vacuously. |
| [#172](https://github.com/moldovancsaba/salesleadgenerator/issues/172) | `Lead.region` type contradicts real free-text behavior | Fixed (2.4.179, PR #184) — live production audit found 55+ real values, widened type to `string`; also converted `AddLeadModal`/`FilterBar`'s hardcoded 3-value `Select` to free-text `TextInput`, matching `country`'s existing pattern. |
| [#179](https://github.com/moldovancsaba/salesleadgenerator/issues/179) | Next.js 16.3.0 GA may fix the two CVEs docs said had no upstream fix | Fixed (2.4.178) — bumped `next`/`eslint-config-next` to 16.3.0, confirmed both CVEs resolved via `npm ls`/`npm audit`. Also resolved 3 unrelated pre-existing `npm audit` findings (`brace-expansion`, `js-yaml`, `nanoid`) via `npm audit fix`. |
| [#178](https://github.com/moldovancsaba/salesleadgenerator/issues/178) | `GET /api/stats` and `GET /api/boards` have no auth check at all | Fixed (2.4.177, PR #181) — gated both with `requireApiKey`, matching every other data-exposing admin route. |
| [#171](https://github.com/moldovancsaba/salesleadgenerator/issues/171) | `Lead` type missing `contactEmails` field | Fixed (2.4.175, PR #177) — added `contactEmails?: string[]` to `app/types.ts`. |
| [#170](https://github.com/moldovancsaba/salesleadgenerator/issues/170) | Sales Settings hardcodes "(€)" on pricing labels regardless of brand | Fixed (2.4.175, PR #177) — labels now derive the symbol from `BRAND_CONFIG[brand].currency` via a new shared `CURRENCY_SYMBOLS` map. |
| [#169](https://github.com/moldovancsaba/salesleadgenerator/issues/169) | Manually-added deals default to USD regardless of brand | Fixed (2.4.171, PR #175) — root cause was deeper than the title: every ticket-size currency computation site ignored the operator's saved Sales Settings currency selection in favor of the brand's fixed default. Fixed at every call site; Sales Settings currency option preserved, now actually honored. |
| [#166](https://github.com/moldovancsaba/salesleadgenerator/issues/166) | 4 bugs/type gaps surfaced during the docs/LLD audit | Split into #169–#172 above for independent tracking. |
| [#163](https://github.com/moldovancsaba/salesleadgenerator/issues/163) | Apply #136's federation/tournament rule to the 4 remaining leads | Resolved — all 4 researched and reclassified with real citations. |
| [#142](https://github.com/moldovancsaba/salesleadgenerator/issues/142) | Reply matching + contact-enrichment suggestions from inbound email | Shipped (2.4.165). Live webhook trigger still pending owner-only infra steps — now tracked as its own issue, [#202](https://github.com/moldovancsaba/salesleadgenerator/issues/202), in Ready above. |
| [#135](https://github.com/moldovancsaba/salesleadgenerator/issues/135) / [#136](https://github.com/moldovancsaba/salesleadgenerator/issues/136) / [#143](https://github.com/moldovancsaba/salesleadgenerator/issues/143) | Taxonomy governance decisions (brand convention, federation/tournament rule, entertainment-event value) | Resolved per owner decision, retroactively applied to known data points. |

---

## Legend

- **Priority**: P1 = high, P2 = medium, P3 = low — matches each issue's own GitHub label.
- **Status groups** here mirror what a project board's columns would show (In Progress / Ready / Blocked) — primarily derived from each issue's own `status:` label, not a separate tracking mechanism, with documented exceptions noted inline where a single issue's real-world state doesn't collapse cleanly into one label.
