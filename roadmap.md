# Roadmap — Sales Lead Generator

**Why this file exists:** GitHub's Projects (v2) board for this repo cannot be read or written from this session — its only API is GraphQL, and this session's GitHub credentials are restricted to a pinned set of PR-review operations (verified directly: a live authenticated GraphQL call was rejected by the session's own proxy, not by GitHub). Classic Projects (the old REST-based board) is gone from GitHub entirely — sunset 2024, fully removed mid-2025 (confirmed live: `GET /repos/.../projects` returns a real `404` from GitHub itself). This file is the substitute: every real open issue, grouped by status, kept in sync by hand whenever an issue's state changes. It is not a replacement for the issues themselves — every row here is a real GitHub issue, this is just a single-page view of all of them.

**Keeping this in sync:** whenever an issue opens, closes, or changes status/priority, update its row here in the same change. If this file and GitHub ever disagree, GitHub is the source of truth — fix this file, not the other way around.

**Full methodology** — the exact tools used to manage issues, the mandatory issue-body structure, the real label taxonomy, how dependencies between issues are recorded, and the complete verified investigation behind why no board is reachable — lives in `docs/ISSUE_MANAGEMENT.md`. This file is just the live status view; that one is the reference.

**Not the same file as `_archived/roadmap.md`** — that's a frozen, historical feature-status log (v2.4.61), superseded by `CHANGELOG.md`, sharing this file's basename by coincidence rather than by relation. See `README.md`'s "Archived Documentation" table.

Last synced: 2026-09-25, against `moldovancsaba/salesleadgenerator`'s real open-issue list (25 issues). This sync follows a full CRM-parity audit (feature gap analysis against Pipedrive/HubSpot/Salesforce/Close/Copper, plus published CRM-UX research) that produced 20 new production-grade issues (#198–#217, all following the canonical structure from `sovereignsquad/general-design-system#81`) and unblocked #125. Every new issue's UI/UX scope is mandated to use only the Sovereign Squad General Design System.

---

## In Progress

| # | Title | Priority | Notes |
|---|---|---|---|
| [#198](https://github.com/moldovancsaba/salesleadgenerator/issues/198) | CRM: Lead ownership - assignedTo field, My Leads view, and reassignment workflow | P1 | **Foundational — most other new-CRM-feature issues below depend on this.** Implemented on `claude/jolly-darwin-uudbie` (PR #218, open/draft, not yet merged): `Lead.assignedTo`/`assignedToEmail`/`assignedAt`/`assignedBy`, session-gated `ASSIGN` action in `executeLeadAction()` (self-assign always allowed, cross-user reassignment gated to brand admins, 403 on block), `assignedTo=me\|unassigned\|<ssoUserId>` filter on `GET /api/leads`/`GET /api/leads/columns`, new `GET /api/leads/assignable-users`, "My Leads" toggle in `FilterBar`. Will move to Done once PR #218 merges. |
| [#199](https://github.com/moldovancsaba/salesleadgenerator/issues/199) | CRM: Team visibility - manager role and team-scoped pipeline access | P1 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged) directly on top of #198. New `teams` collection + `lib/teams.ts` (additive to `lib/sso-access.ts`'s flat brand-role model, never modifying it); `assignedTo=team` added onto #198's own `assignedTo` param (reconciled from the issue's draft `scope=team` once #198's actual shipped convention was known — its own §24 flagged this). New `GET/POST /api/admin/teams` + `PATCH/DELETE /api/admin/teams/[teamId]` (super-admin-gated), new `/admin/teams` UI, "My Team" toggle in `FilterBar`. Will move to Done once PR #218 merges. |
| [#200](https://github.com/moldovancsaba/salesleadgenerator/issues/200) | Activity: Manual call logging - structured Call activity type with outcome disposition | P1 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). `activityLog` (from #140/#141/#142) gains a `type: 'call'` variant — closed `CallDisposition` enum, `matchedContactKey` reuse, `loggedBy` from the verified session. New `POST /api/leads/[id]/activity` (alongside the existing `GET`); logging a call touches the lead's `updatedAt` so it counts toward the existing rotten-indicator/staleness computation. "Log a call" form in `ActivityPanel.tsx`, disabled with a tooltip for a zero-contact lead. Will move to Done once PR #218 merges. |
| [#125](https://github.com/moldovancsaba/salesleadgenerator/issues/125) | Adopt GDS zone-based kanban scroll routing (`columnPanZone`); retire local wheel-passthrough workaround | P2 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). `app/kanban.tsx`'s `<GdsKanbanBoard>` now sets `columnPanZone="header"`; `lib/desktop-scroll-passthrough.ts` and its test deleted (grep-confirmed no other importer). Real-device trackpad confirmation still needs the owner — disclosed, not removed from `docs/OPERATOR_GUIDE.md`'s Known Issues per the issue's own instruction. Will move to Done once PR #218 merges. |
| [#203](https://github.com/moldovancsaba/salesleadgenerator/issues/203) | Kanban: Bulk actions v2 - contextual toolbar, undo, and bulk reassignment | P2 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). `ALLOWED_BULK_ACTIONS` widened to `FIELD_EDIT` (tags add/remove, qualityStatus set) and `ASSIGN` (unblocked by #198, shipped for real rather than left inert). New Mongo-backed, TTL-indexed `bulkActionUndoTokens` + `POST /api/leads/bulk/undo` — CAS-checked, single-use, explicit counter reversal, DECLINE's cancelled-cadence side effect flagged `notReversible` and never resumed. "Select all in column", "Edit field…", "Reassign…", and a real Undo control added to `app/kanban.tsx`. Will move to Done once PR #218 merges. |
| [#204](https://github.com/moldovancsaba/salesleadgenerator/issues/204) | Forecast: Rep-editable probability - forecast categories and quota attainment tracking | P2 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). New rep-editable `Lead.forecastCategory` (`pipeline\|best_case\|commit\|closed`, stage-derived default, sticky override via new `SET_FORECAST_CATEGORY` action) plus additive `categoryWeightedRevenue`/`byCategory`/`categoryWeightsUsed` in `computeForecast()` (`app/lib/forecast.ts`) — stage-weighted total unchanged. Quota-tracking half, previously spec'd as inert pending `Lead.assignedTo`, now unblocked by #198 (shipped 2.4.189) and implemented for real: new `quota_targets` collection, `GET/PUT /api/quota/[brand]` (super-admin), `GET /api/quota/[brand]/attainment` (self-view always allowed, cross-user admin-only), real closed-won revenue computed via a WON-lead/`outcomelogs` join. UI: Forecast-category control on lead detail, combined attainment tile/admin quota form on the Forecast page. Will move to Done once PR #218 merges. |
| [#201](https://github.com/moldovancsaba/salesleadgenerator/issues/201) | Automation: Workflow engine - trigger-action rules generalizing cadence-tick and stage-gate | P2 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). New `automation_rules` collection + flat trigger→action engine (`lib/automation-rules.ts` pure logic, `app/lib/automation-store.ts` Mongo evaluation), generalizing `lib/cadences.ts`'s tick model rather than replacing it. `lead_created`/`lead_moved_to_column` fire synchronously from their real write points (the latter only after the stage gate passes); `stale_no_activity` runs on a new daily cron `GET/POST /api/admin/automation-tick`, reusing `lib/stale-deal.ts`'s `computeStaleness()` verbatim, idempotent per UTC day via new `automation_rule_firings`. `lead_assigned` kept schema-defined-but-unreachable exactly as specced — unlike #203/#204's own now-unblocked-by-#198 corrections, this issue's own §23 explicitly scopes a real per-user assignment/notification model as a separate future issue, not a fold-in. New CRUD `GET/POST /api/automation-rules` + `GET/PUT/DELETE /api/automation-rules/[id]`, new `/automation/[brand]` UI, new nav entry. Will move to Done once PR #218 merges. |
| [#205](https://github.com/moldovancsaba/salesleadgenerator/issues/205) | Outreach: One-off tracked email send - decoupled from cadence automation | P2 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). `lib/outreach-send.ts` generalized into a shared `dispatchOutreachEmail()` core; `sendAutomatedEmail()` (cadence) unchanged, new `sendManualEmail()` for a new "Send email" button in the compose modal (also fixes a real Rule 7 mislabeling — "Log outreach" used to say "Sending…" while never sending). New `POST /api/outreach-send`. A real send-timeline duplication bug caught and fixed before shipping (the issue's own diagram would have double-rendered a send in the Activity tab — fixed via a new `activityLogWritten` marker). Extends the existing inbound-webhook endpoint's Resend subscription for delivery/open/click tracking, retry-deduped on `svix-id`. The issue's own real-send manual-verification step is disclosed as owner-blocked (no `RESEND_API_KEY` in this sandbox). Will move to Done once PR #218 merges. |
| [#206](https://github.com/moldovancsaba/salesleadgenerator/issues/206) | Contacts: Buying-committee roles - replacing isDecisionMaker with a role enum | P3 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). Additive `buyingRole` enum (`economic_buyer\|champion\|influencer\|blocker\|decision_maker\|unknown`) per contact in `Lead.contacts[]`, resolved via `resolveBuyingRole()`/`deriveIsDecisionMaker()` inside `lib/contacts.ts`'s `normalizeContact()`, the one function every write path (`POST`/`PUT`/`PATCH MODIFY`) already shares — `isDecisionMaker` stays fully accepted on input, now permanently derived on output. `dedupeContacts()`'s collision-merge gained matching precedence (a duplicate only ever upgrades an `unknown` survivor). Confirmed and regression-tested that `lib/stage-gate.ts`'s ENGAGED/PROPOSAL gate stays generic ("a contact", not role-specific) — untouched. Idempotent backfill shipped (`lib/backfill-buying-role.ts` + `POST /api/admin/buying-role-backfill`), mirroring `lib/backfill-title-normalization.ts`'s shape; correctness doesn't depend on running it. UI: `ContactsEditor.tsx`'s checkbox replaced by a role select; per-role badges in `app/detail.tsx`/`app/contacts/[brand]/contacts-client.tsx`. Will move to Done once PR #218 merges. |
| [#209](https://github.com/moldovancsaba/salesleadgenerator/issues/209) | Accounts: Company object - navigable parent-organization record with rollup | P3 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). Groups existing `Lead.parentOrgId`/`parentOrgName`/`relationshipToParent` (added in closed #131) into a rep-navigable Account view (`/accounts/[brand]`) with a computed rollup (lead count, stage breakdown, pipeline/won value, contact count, most recent activity), reusing the `AdminDataTable`/`AdminDetailDrawer`/`AdminModal` pattern from `app/contacts/[brand]`. Phase 1 only, as specced: pure `lib/accounts.ts` grouping/rollup math, no new Mongo collection, no migration — real `accounts` collection + merge-queue UI explicitly deferred to a follow-on issue. A real spec contradiction (§9 vs §15 on currency handling) found and resolved by following §15's explicit no-conversion exclusion rule, disclosed in `docs/ARCHITECTURE.md`. Depends loosely on #132's ongoing taxonomy backfill for `parentOrgId` coverage. Will move to Done once PR #218 merges. |
| [#215](https://github.com/moldovancsaba/salesleadgenerator/issues/215) | Catalog: Product and price book - reusable line items feeding Deals and quotes | P3 | Implemented on `claude/jolly-darwin-uudbie` (same PR #218, open/draft, not yet merged). New per-brand/tenant `products` Mongo collection (`app/lib/products.ts`) promoting Sales Settings' free-text `ProductLine[]` into a priced, selectable catalog; `lib/deals.ts`'s `Deal` gains an optional `lineItems`, server-derived `value`/`source: 'catalog_line_items'` winning over any client-sent value, bare-value path fully unchanged. One-time idempotent backfill (`lib/backfill-products.ts` + `POST /api/admin/products-backfill`), never clobbers a manually-edited row. New `/admin/products/[brand]` `AdminDataTable`/`AdminModal` CRUD screen (this app's first real `AdminModal` create/edit form) and a "Build from catalog" mode in `app/detail.tsx`'s deal editor. Prerequisite for #211's deferred multi-line-item quote support. Will move to Done once PR #218 merges. |
| [#132](https://github.com/moldovancsaba/salesleadgenerator/issues/132) | Backfill existing leads into the controlled taxonomy schema (rulebook v1.0, Phase 2) | P1 | 318 of 2,874 leads classified as of the last batch (2026-08-08), re-derived live from `GET /api/leads` rather than trusted from any prior branch's own claim (see `docs/LESSONS_LEARNED.md` §3 — an orphaned branch's claimed progress was found not to match production). Picking now weighted toward QUALIFIED/ENGAGED/PROPOSAL-stage leads (sales-team-active), not just backlog — see `docs/LEAD_TAXONOMY_MIGRATION_PLAN.md` §9. Real, ongoing, multi-session effort — not completable in one sitting. |

## Ready (unblocked, unstarted)

| # | Title | Priority | Notes |
|---|---|---|---|
| [#208](https://github.com/moldovancsaba/salesleadgenerator/issues/208) | Kanban: Real drag-and-drop - GDS `enableDrag` activation wired to persisted stage and order changes | P1 | Owner-confirmed flagship epic. Activates GDS `enableDrag` (available since GDS 3.11.0; repo pinned `^6.5.0`) behind a new Mongo-backed kill switch (`settings.{key:'kanban_drag_enabled'}`, same convention as `pipeline_weights`/`stale_thresholds`); cross-column drag reuses the existing `executeLeadAction`/`COLUMN_MOVE` path unchanged; adds a new `COLUMN_REORDER` action for same-column reorder in the 5 manually-controlled columns (fractional-indexing `sortOrder`), rejects same-column drag on the 2 auto-managed (score-sorted) columns with visible feedback. Treats the 2.4.10–2.4.17 production crash (never root-caused to a specific line) as a fresh risk given this repo is 3 GDS major versions past that era — requires staged real-device verification before rollout, not an assumed-safe retry. References #125 as a related-but-separate fast-follow (GDS 3.14.12's `columnPanZone`, confirmed already available at `^6.5.0`), deliberately not bundled into this issue's own scope. Not started. |
| [#202](https://github.com/moldovancsaba/salesleadgenerator/issues/202) | Ops: Inbound email activation - DNS cutover and live verification of the built webhook | P1 | Follows closed #141/#142 (webhook + reply matching, fully built and tested, never live). Needs owner-only steps: `MX leads.haho.ai` DNS record, `RESEND_API_KEY`/`RESEND_WEBHOOK_SECRET` in Vercel production, re-verification of the existing Resend `email.received` webhook subscription, and (for the intended automatic flow) a Google Workspace routing rule. **Needs the owner**, not another agent turn — this session has no access to any of the four systems involved. |
| [#137](https://github.com/moldovancsaba/salesleadgenerator/issues/137) | Duplicate lead records at scale: 43.8% of Seyu, 10.7% of CogMap | P1 | Root-cause matching-algorithm fix already shipped. What remains needs a real browser session at `/admin/duplicates` (super-admin SSO) — confirmed blocked via both `x-api-key` and direct MongoDB attempts. **Needs the owner**, not another agent turn. |
| [#165](https://github.com/moldovancsaba/salesleadgenerator/issues/165) | New-user onboarding tour: step-by-step spotlight walkthrough (design record) | P3 | Design plan, decision made 2026-08-08 (`driver.js`). Implementation (#185) shipped 2.4.187 — this issue stays open by explicit owner design as the permanent design record, not because anything remains to be done. |
| [#214](https://github.com/moldovancsaba/salesleadgenerator/issues/214) | Filters: Shareable saved views - synced, team-visible filter presets | P3 | Moves saved filter presets (`lib/saved-filters.ts`, `app/lib/saved-filters-storage.ts`) from per-browser `localStorage` to a server-persisted `saved_filters` Mongo collection keyed `{brand, ssoUserId, name}`, plus an optional `sharedWithBrand` flag gated on brand-admin role. Includes a one-time, explicit "import your local saved filters" migration action — no silent data loss. Does not depend on #199's team concept; only a future team-manager-scoped sharing widening would. Not started. |
| [#207](https://github.com/moldovancsaba/salesleadgenerator/issues/207) | Calendar: Meeting scheduler - booking links and two-way calendar sync | P2 | Single-provider (Google Calendar) v1: a per-rep OAuth grant structurally parallel to but fully separate from `lib/sso.ts`'s SSO grant, a public `/schedule/[bookingSlug]` booking page, and extends `ActivityEntryType`/`ActivitySource` (#140) with `meeting-scheduled`/`calendar-sync` rather than a new activity concept. Two-way sync reuses the exact `nextActionDueAt`/`nextActionNote` write pattern already established by `app/api/admin/cadence-tick/route.ts:158-161` (#121). Its originally-specced bespoke `calendar_connections` collection should be superseded by / built on #217's shared `integration_connections` model once that lands (both build orderings are viable, noted in-issue); Calendly is flagged as a future alternative provider (its simpler personal-access-token shape maps to #217's `authMethod: 'api_key'`) but explicitly kept out of this issue's Google-only v1 scope. Not started. |
| [#211](https://github.com/moldovancsaba/salesleadgenerator/issues/211) | Deals: Quote generation - line-item documents with e-signature status tracking | P2 | New `quotes` Mongo collection + `draft → sent → viewed → signed` state machine off `lib/deals.ts`'s `Deal`. v1: one line item snapshotted 1:1 from the source `Deal` (multi-line deferred to #215, "Catalog: Product and price book," now filed), PDF via `@react-pdf/renderer` (new dependency — none exists today), stored via `@vercel/blob` (new dependency + a `BLOB_READ_WRITE_TOKEN` this repo doesn't have provisioned yet), sent via the existing `lib/outreach-send.ts` Resend path (no parallel email code). No native e-signature capture in v1 — a token-gated shareable link the rep sends and manually marks `signed`; a third-party integration (Dropbox Sign API recommended over DocuSign on 2026 cost comparison) is scoped as an explicit fast-follow, not built here. Not started. |
| [#213](https://github.com/moldovancsaba/salesleadgenerator/issues/213) | UX: Kanban card density - information hierarchy, WIP signaling, and command palette | P2 | Two independently shippable deliverables: (1) `app/card.tsx`'s `LeadCard` collapses from 14 always-visible signal types today down to a prioritized Tier 0/Tier 1 (in-card expand) split, plus a non-blocking configurable WIP-limit cue on column headers (`app/kanban.tsx`, via GDS's documented `title: ReactNode` support — no fork); (2) a desktop-only Cmd/Ctrl+K command palette adopting GDS's own already-shipped `CommandRegistryProvider`/`useCommandLauncher`/`CommandPalette` (`packages/gds-core/src/CommandPalette.client.tsx`, read directly — this app had never adopted it), reusing the existing `GET /api/search` predictive endpoint and `useAuth()`'s brand-access data. Two real upstream GDS gaps found and flagged rather than worked around: no exposed query-change hook (blocks live per-keystroke server search inside the palette, so v1 covers only already-loaded leads) and no arrow-key list navigation (Tab-only today). `phase: 1-core-parity`. Not started. |
| [#217](https://github.com/moldovancsaba/salesleadgenerator/issues/217) | Integrations: Third-party connection hub - admin-managed credentials for Google Workspace and Calendly | P2 | Owner directive: always consider 3rd-party integrations (Google Contacts/Calendar/Gmail, Calendly, etc.) with user-supplied credentials. New shared `integration_connections` collection (`{brand, tenantId, provider, authMethod: 'oauth2'\|'api_key', encryptedCredentials, status, ...}`, AES-256-GCM via a new `INTEGRATION_CREDENTIALS_ENCRYPTION_KEY`) and an admin Integrations page at `/salessettings/[client]/integrations`, generalizing `lib/sso.ts`'s PKCE OAuth helper for the Google family and supporting Calendly's personal-access-token model (verified against Calendly's real API docs) via `authMethod: 'api_key'`. Meant to supersede #207's bespoke `calendar_connections` design (cross-referenced there) and is the credential-storage foundation #216 builds on; resolved a route-naming collision with #210 by using `/salessettings/[client]/integrations` instead of `/admin/integrations`. Not started. |
| [#216](https://github.com/moldovancsaba/salesleadgenerator/issues/216) | Integrations: Gmail and Google Contacts sync - two-way email and contact data flow | P3 | Depends on #217 for OAuth/credential plumbing. v1 Gmail: periodic (not push) polling of a connected rep's inbox, matched to leads via the existing `contactEmails[]`/`matchReplyToLeads()` (unmodified), written into the shared `activityLog` under a new `source: 'gmail-sync'`, deduped by Message-ID against anything already captured via the existing Resend inbound path. v1 Google Contacts: a rep-initiated one-way "Import from Google Contacts" action through the existing `contactKey()`/`dedupeContacts()` path — no automatic continuous two-way sync (deferred; conflict resolution better suited to a human-reviewed flow like `/admin/duplicates`). Does not touch outbound send (stays on Resend, per #205). Not started. |
| [#212](https://github.com/moldovancsaba/salesleadgenerator/issues/212) | Reporting: Ad-hoc report builder - user-defined reports with scheduled delivery | P3 | Generalizes the fixed-shape Metrics dashboard (`app/metrics.tsx`, `/api/metrics/*`) into a closed-allowlist {metric, group-by, filters, date range} form driving a new pure `lib/report-pipeline.ts` Mongo aggregation builder (zero raw-query-injection surface), plus a `report_definitions` collection with an optional recurring schedule delivered via a new Vercel Cron tick reusing `CRON_SECRET`/`requireCronOrApiKey` and the existing Resend-send convention (mirrors `lib/cadences.ts`/cadence-tick exactly). Flags a real, verified gap found while scoping this: no index currently exists on any per-brand leads collection for `{tenantId, createdAt}`/`{tenantId, kanbanColumn}` — mandates adding them as part of this work. Chart output must use GDS's governed chart types only. Not started. |
| [#210](https://github.com/moldovancsaba/salesleadgenerator/issues/210) | API: Public integration surface - OAuth-scoped API access and outbound webhooks | P3 | Replaces the single shared `SLG_API_KEY` (used today by the research agent and all 3 Vercel Cron jobs) with per-brand, per-scope, revocable, hash-stored API keys (`api_keys` collection) migrated via a time-boxed dual-auth window, not a hard cutover. Adds outbound webhooks (`lead.created`/`lead.stage_changed`/`lead.won`/`lead.lost`) signed HMAC-SHA256, mirroring the existing inbound Resend-verification pattern in `lib/resend-webhook.ts`, with a 5-attempt retry schedule and auto-disable-after-failures dead-letter policy. Distinct from #217: this issue is about *other systems* authenticating *into* this app; #217 is about this app connecting *out* to third-party SaaS. Not started. |

## Blocked

Nothing currently blocked. (#125 moved fully into Ready above on 2026-09-25 — its upstream dependency, GDS `columnPanZone`, was verified already shipped and already installed in this repo; see its row above.)

## Backlog

| # | Title | Priority | Notes |
|---|---|---|---|
| [#193](https://github.com/moldovancsaba/salesleadgenerator/issues/193) | Non-conforming `lead.source` values: 2,739 of 3,051 leads don't match the documented channel vocabulary | P3 | Not a validation gap — `source` is deliberately freeform (issue #123). Data-quality backlog: `csv_import` (1730), missing/`unknown` (977), and a handful of dvsc process-artifact strings need an owner decision on reclassification before any bulk write. Numbers re-derived live against production 2026-08-20, reconciled exactly against `GET /api/health`'s lead counts. |

## Recently closed (context)

| # | Title | Resolution |
|---|---|---|
| [#197](https://github.com/moldovancsaba/salesleadgenerator/issues/197) | Near-zero lead review rate: add bulk Accept + inline card Accept/Decline to the kanban | Shipped (2.4.188). `PATCH /api/leads/bulk`'s `ALLOWED_BULK_ACTIONS` widened to include `ACCEPT`; kanban toolbar gained a bulk "Accept selected" button; `app/card.tsx` gained inline per-card Accept/Decline, wired through a new `onAction` prop to the existing `handleAction`/`PATCH /api/leads` path. Purely additive UI wiring onto already-audited backend paths (`leads-patch-actions.integration.test.ts`) — no data model or stage-gate change. Full gate green: 743/743 unit, 228/228 integration, 0 `tsc`/lint errors, 0 new `audit:gds-style` violations (stash A/B against the 26 pre-existing). |
| [#185](https://github.com/moldovancsaba/salesleadgenerator/issues/185) | Implement new-user onboarding tour using driver.js | Shipped (2.4.187, `4ba527a`, merged to `main`). All 7 steps from #165's design, two real UI-driving transitions (click-simulated, not just spotlight-and-describe on closed elements). Two real `driver.js` gotchas found by reading its compiled source and designed around: the auto-trigger is route-gated (a fresh login lands on `/forecast/[brand]`, not `/sales/[brand]` where the tour lives) and `onDestroyStarted` must call `driver.destroy()` itself or Escape/close silently no-ops. Storage is server-persisted (`SsoUserAccessRecord.tourSeenAt`), not localStorage. New `tests/lib/tour-logic.test.ts` (8 tests) for the pure logic. Authenticated auto-trigger path needs a real SSO login to fully exercise, disclosed — same limitation as #195/#196. Full gate green including a clean `next build`. |
| [#196](https://github.com/moldovancsaba/salesleadgenerator/issues/196) | Site-admin UI to create a new client/brand (no-deploy onboarding) | Shipped (2.4.186, `118be98`, merged to `main`). `/admin/clients` create form + `GET`/`POST /api/admin/clients`, uniqueness via `brands`'s own unique index. Along the way, found and fixed a real bug already live on `main` since #195: `app/lib/brand.ts` importing `lib/mongodb.ts` broke the client bundle for any Client Component pulling a real value (not just a type) from it — confirmed via `next build` that `/salessettings/[client]` was already broken by the #195 merge itself. Fixed by splitting pure constants into `app/lib/brand-constants.ts` + adding `server-only` to `brand.ts`. Migration script (`scripts/migrate-brands-to-mongo.ts`, from #195) still not run against production — no live Mongo access from this session; disclosed in both issues' resolution comments. Full gate green including a clean `next build`. |
| [#195](https://github.com/moldovancsaba/salesleadgenerator/issues/195) | Brand config becomes Mongo-backed (site-admin onboarding, backend plumbing) | Shipped (2.4.185, `fff6644`, merged to `main`). New `brands` Mongo collection + async `getBrandConfig`/`getAllBrandConfigs`/`resolveBrand`/`getForbiddenTermsFor`; grep-verified 51-file call-site sweep; forecast-model routing and forbidden-vocabulary derivation moved off hardcoded brand-name checks. Migration script not yet run against production (no live Mongo access from this session) — app runs correctly off the fallback config until it is. Full gate green. Prerequisite for #196, shipped in the same session. |
| [#194](https://github.com/moldovancsaba/salesleadgenerator/issues/194) | `POST /api/search-learning` 500s on first write for a new `companyId` (Mongo upsert path conflict) | Fixed (2.4.184) — `$setOnInsert` seeded `searchRuns`/`lastQueries` while `$inc`/`$push` targeted the same paths; MongoDB rejects that (code 40) so the insert branch never ran. Both seeds removed; the operators initialize their own fields. Found live while verifying #192, predates it. New `tests/integration/search-learning.integration.test.ts` verified to fail against the pre-fix code (3 of 4 cases 500), not merely pass after. |
| [#192](https://github.com/moldovancsaba/salesleadgenerator/issues/192) | Unauthenticated read/write exposure across search, boards, forecast, metrics, win-rates, ticket-size-calibration, and settings | Fixed (2.4.183, `ad53eaa`) — same gap class as #104/#178, found not to have generalized (`docs/LESSONS_LEARNED.md` §3). `requireBrandAccessApi` on 7 brand-scoped GET routes + `GET /api/search`'s brand mode; `requireSuperAdminSession` on `/api/search`'s no-brand cross-all-brands mode; new `requireApiKeyOrSession` guard on `PUT /api/settings`/`POST /api/search-learning`. Full gate passing and live-verified against a local dev server with real credentials before push. Production deploy status unconfirmed (no reachable Vercel account from this session). |
| [#188](https://github.com/moldovancsaba/salesleadgenerator/issues/188) | Per-field provenance on leads: `fieldVerifications` with a closed method enum | Merged to `main` (`03446dc`) and closed on GitHub. Additive only — new optional field at two scopes (lead-level for scalar fields, per-contact for contact fields), closed nine-value method enum, bounded by last-write-wins per `(field, method)` plus a 60-entry cap. |
| [#189](https://github.com/moldovancsaba/salesleadgenerator/issues/189) | `main` is red: credential scrub left webhook test fixtures undecodable | Fixed (PR #190) — the 2026-08-14 scrub replaced webhook test fixtures with a placeholder that isn't valid base64, so `standardwebhooks`' eager decode threw before any assertion ran (14 tests failing). Fixture is now derived in-file from a fixed byte pattern: decodable, deterministic, obviously synthetic, no literal for a scanner to match. Also restored two negative tests that had been given the same placeholder as the valid secret and would have passed vacuously. |
| [#172](https://github.com/moldovancsaba/salesleadgenerator/issues/172) | `Lead.region` type contradicts real free-text behavior | Fixed (2.4.179, PR #184) — live production audit found 55+ real values, widened type to `string`; also converted `AddLeadModal`/`FilterBar`'s hardcoded 3-value `Select` to free-text `TextInput`, matching `country`'s existing pattern. |
| [#179](https://github.com/moldovancsaba/salesleadgenerator/issues/179) | Next.js 16.3.0 GA may fix the two CVEs docs said had no upstream fix | Fixed (2.4.178) — bumped `next`/`eslint-config-next` to 16.3.0, confirmed both CVEs resolved via `npm ls`/`npm audit`. Also resolved 3 unrelated pre-existing `npm audit` findings (`brace-expansion`, `js-yaml`, `nanoid`) via `npm audit fix`. |
| [#178](https://github.com/moldovancsaba/salesleadgenerator/issues/178) | `GET /api/stats` and `GET /api/boards` have no auth check at all | Fixed (2.4.177, PR #181) — gated both with `requireApiKey`, matching every other data-exposing admin route. |
| [#171](https://github.com/moldovancsaba/salesleadgenerator/issues/171) | `Lead` type missing `contactEmails` field | Fixed (2.4.175, PR #177) — added `contactEmails?: string[]` to `app/types.ts`. |
| [#170](https://github.com/moldovancsaba/salesleadgenerator/issues/170) | Sales Settings hardcodes "(€)" on pricing labels regardless of brand | Fixed (2.4.175, PR #177) — labels now derive the symbol from `BRAND_CONFIG[brand].currency` via a new shared `CURRENCY_SYMBOLS` map. |
| [#169](https://github.com/moldovancsaba/salesleadgenerator/issues/169) | Manually-added deals default to USD regardless of brand | Fixed (2.4.171, PR #175) — root cause was deeper than the title: every ticket-size currency computation site ignored the operator's saved Sales Settings currency selection in favor of the brand's fixed default. Fixed at every call site; Sales Settings currency option preserved, now actually honored. |
| [#166](https://github.com/moldovancsaba/salesleadgenerator/issues/166) | 4 bugs/type gaps surfaced during the docs/LLD audit | Split into #169–#172 above for independent tracking. |
| [#163](https://github.com/moldovancsaba/salesleadgenerator/issues/163) | Apply #136's federation/tournament rule to the 4 remaining leads | Resolved — all 4 researched and reclassified with real citations. |
| [#142](https://github.com/moldovancsaba/salesleadgenerator/issues/142) | Reply matching + contact-enrichment suggestions from inbound email | Shipped (2.4.165). Live webhook trigger still pending owner-only infra steps — now tracked as its own issue, [#202](https://github.com/moldovancsaba/salesleadgenerator/issues/202), in Ready above. |
| [#135](https://github.com/moldovancsaba/salesleadgenerator/issues/135) / [#136](https://github.com/moldovancsaba/salesleadgenerator/issues/136) / [#143](https://github.com/moldovancsaba/salesleadgenerator/issues/143) | Taxonomy governance decisions (brand convention, federation/tournament rule, entertainment-event value) | Resolved per owner decision, retroactively applied to known data points. |

---

## Legend

- **Priority**: P1 = high, P2 = medium, P3 = low — matches each issue's own GitHub label.
- **Status groups** here mirror what a project board's columns would show (In Progress / Ready / Blocked) — primarily derived from each issue's own `status:` label, not a separate tracking mechanism, with documented exceptions noted inline where a single issue's real-world state doesn't collapse cleanly into one label.
